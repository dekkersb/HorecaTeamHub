import React from 'react';
import "./ChangeButton.css";

function ChangeButton() {
    return (
        <div>
            <button  className={"changeButton"}>Wijzigen</button>
        </div>
    );
};

export default ChangeButton;